# 版本控制审计记录

更新时间：2026-07-16（Asia/Shanghai）

## 分支用途

- `main`：仅接收通过完整构建与交互验收的可发布版本；当前尚未创建。
- `integration/desktop-pet`：保存通过技术门禁、仍待用户交互验收的完整集成快照。
- `feature/*`：后续用于隔离未完成、构建失败或存在冲突的单项功能。

## 基线审计

- 远端 `origin`：`https://github.com/KrikleChen/-.git`；首次 fetch 时无分支或标签，属于空仓库。
- 纳管范围：20 个 Swift 源文件、10 个 Swift 工具脚本、58 个 PNG 原始/处理后资产、`Info.plist`、`build.sh` 与项目文档。
- 排除范围：`build/`、Swift/Xcode 可再生构建状态、系统元数据、日志和临时文件。
- 敏感信息扫描未发现密钥、token、密码、私钥或本机绝对路径；非构建目录未发现超过 20 MiB 的文件。

## 构建门禁

构建命令：

```sh
./build.sh
```

当前结论：集成分支技术门禁通过；本轮交互记忆、碰撞分级、道具回收和边缘待机仍需人工 QA，背景权限与名字监听仍需真实授权环境验收，因此不允许进入 `main`。

2026-07-16 复审结果：

- `./build.sh` 成功，`codesign --verify --deep --strict` 成功，应用启动后未出现即时崩溃。
- x86_64 与 arm64、macOS 12 target 的全工程 `-warnings-as-errors` 类型检查均成功。
- 抓取区域测试和交互手势测试成功；甩飞阈值为 720 points/s，使用最后 0.16 秒轨迹。飞行方向只由窗口物理轨迹表现，不再对人物图层叠加方向矩阵；用户确认左右甩飞全程可见、方向正确。
- 台词门禁通过：除 `ObjectionBurstView` 的“異議あり！”演出外，人物区域固定为 `y=0...180`，所有普通正文统一为顶部字幕卡和 13 pt semibold；场景只改变左侧标记、强调色及入退场动画，用户确认不遮挡人物。
- 挣扎字幕停留阶段静止，人物自身挣扎动画保持不变；碰撞字幕 0.08 秒淡入后静止，0.65 秒后才替换下一句，用户确认“痛！”稳定可读。
- “今日一案”已按用户决定完整删除：源码、右键菜单、交互模式和法庭记录条目均无残留；用户确认菜单与图鉴中不再出现相关入口。
- 法庭记录图鉴、记录次数累加及 P0 面板交互通过人工验收，面板不会抢占输入焦点。
- 交互记忆自检通过：18 秒窗口内拖拽按 normal、irritated、resigned 阶段升级；10 秒窗口内名字回应按 first、again、exasperated 三段升级。
- 碰撞分级自检通过：使用 restitution 和阻尼之前的法向入射速度区分 light、medium、heavy，并按最重撞击选择落地恢复；右下角 `(600, -1300)` 回归样例保持 heavy。
- 散落道具回收自检通过：仅落地后首次左键可回收，非激活面板不会成为 key/main window，回收开始时动态读取桌宠当前手边位置。
- 边缘待机自检通过：仅支持左、右、底部，具备最低空闲时间和全局冷却；真实触发记录 `edgeRest`，隐藏预览同时禁止 `edgeRest` 与 `.think` 图鉴写入。隔离存储测试和应用 E2E 均确认预览前后计数不变。
- 角落碰撞的完整入射速度与预览图鉴隔离两项回归已补测试并通过。
- 6 个 `scatter-*-cg.png` 均带 alpha 且已进入 app bundle；新增资源总计约 2.9 MB，未发现异常大文件。
- 敏感信息、本机绝对路径、临时产物和 diff 空白检查均通过。
- 静态复审未发现甩飞途中重新抓取、落地恢复、倒吊散落清理、异步背景结果防陈旧、监听状态接线或图鉴次数累计中的明显阻塞性回归。

## 待验收项

- 用户验收 18 秒内连续拖拽的三阶段反应，以及 10 秒内连续名字呼叫的三段回应。
- 用户验收 light、medium、heavy 碰撞字幕与对应落地恢复链。
- 用户验收散落道具落地点击、飞回当前手边、接住反馈和不抢焦点。
- 用户验收左/右探身、底部下沉遮腿、低频触发和冷却节奏。
- 在真实录屏授权环境验收 ScreenCaptureKit 背景落点识别。
- 在真实辅助功能授权及目标聊天输入框中验收姓名监听。
- 完成上述人工验收后，才可评估将集成分支合入并推送 `main`。
